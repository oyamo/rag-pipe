name: Build and Push Docker Images to GHCR

on:
  push:
    branches: [ main ]
    tags: [ 'v*.*.*' ]

jobs:
  build-and-push:
    name: Build and Push Microservices to GHCR
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Ingestion Image
        uses: docker/build-push-action@v5
        with:
          context: ./ingestion
          file: ./ingestion/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/ingestion:latest
            ghcr.io/${{ github.repository_owner }}/ingestion:v0.1
            ghcr.io/${{ github.repository_owner }}/ingestion:${{ github.sha }}

      - name: Build and Push Inference Image
        uses: docker/build-push-action@v5
        with:
          context: ./inference
          file: ./inference/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/inference:latest
            ghcr.io/${{ github.repository_owner }}/inference:v0.1
            ghcr.io/${{ github.repository_owner }}/inference:${{ github.sha }}

      - name: Build and Push Pipe Image
        uses: docker/build-push-action@v5
        with:
          context: ./pipe
          file: ./pipe/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/pipe:latest
            ghcr.io/${{ github.repository_owner }}/pipe:v0.1
            ghcr.io/${{ github.repository_owner }}/pipe:${{ github.sha }}
